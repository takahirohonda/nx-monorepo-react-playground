#import "./ProductFields2.graphql"
query GetProducts($pageSize: Int!) {
  site {
    products(first: $pageSize) {
      edges {
        node {
          ...ProductWithVariants
        }
      }
    }
  }
}
