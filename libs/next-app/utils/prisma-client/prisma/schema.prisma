// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite" 
  url      = "file:/Users/taka/code/mdh/nx-monorepo-react-playground/libs/next-app/utils/prisma-client/prisma/dev.db" // this should work according to the doc...
}

model Todo {
  id String @id @default(uuid())
  createdAt DateTime @default(now())
  content String
  completed Boolean @default(false)
  more String
}
